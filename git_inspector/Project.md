Build a tool that accepts lists of Github Username, fetches their user profile concurrently from Github API, and calculate the average number of repo
**Constraints**
- You must use Async/Await (tokio) and serialization
- You can't use std::spawn::thread